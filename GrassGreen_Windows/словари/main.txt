**Замена python

log = вывести что то на экран
chek = это просто замена if else elif 
fany = это подсчёт чего то типо 3+5=8 
!" " = это обазначение библиотеки 
true =  ну это уже понятно что типо правда
false =  ну это тоже что типо не правда
* = это просто чтоб сделать однострочный комментарий
** ** = это уже чтоб сделать комментарий на несколько строе
.// = вот с помощью этой команды можно запустить наш скрипт
use 601 = это использования русского языка utf-8
use utf-8 = это тоже но просто проще
set = это переменная 
again = это повторяющийся цикл
stoped = это чтоб остановить цикл
do(..) = это макрос и в скобки пишешь букву на клавиатуре и когда будет нажимать на неё то она сделает
так как ты неё запрограммировал
invent[] = ну это списки 
ask() = это спрашивает у пользователя
connect() = вот оно подключистя к какому то файлу или папке для макросов и читов или к примеру
сделать стабильное подключение к серверу
creat() = она создаст какой нибудь файл или ещё что то
redact() = вот он сможет отредактировать уже любой файл в памяти
wait() = с помощью этого можно остановить скипт на сколько то времени
exit = с помощью этого завершается программа
id() = это айди для удобства чтоб какую нибудь строку или слово можно было задействовать несколько раз
open() = с помощью этого уже можно открывать файлы 
/n = ну это как в питон просто чтоб перешлось на следущую строку
@text_off = это как в батч echo off чтоб не отабражался текст
text; = с помощью этого можно будет корректно сделать чтоб строка была пустая 
